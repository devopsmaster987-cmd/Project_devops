apiVersion: v1
kind: Service
metadata:
  labels:
    app: db-service
  name: vprocache01
spec:
  ports:
  - name: db-service
    port: 8080
    protocol: TCP
    targetPort: app_port
  selector:
   app: vproapp
  type: LoadBalancer